# State Management

Use the smallest state tool that fits:

- Component state for local ephemeral UI.
- Zustand for cross-component local UI/app state.
- TanStack Query for server state and cache.
- URL/router state for navigational state.

Do not duplicate server state in Zustand.
