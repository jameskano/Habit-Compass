# Harness Strategy

Build reusable harnesses for repeatable feature tests.

- Domain fixtures live in `src/test/fixtures`.
- Render helpers live in `src/test/utils`.
- Feature harnesses live in `src/test/harnesses`.
- Prefer user-observable assertions.
