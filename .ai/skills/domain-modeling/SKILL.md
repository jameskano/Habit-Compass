# Domain Modeling

Use this skill when defining habit, task, category, mood, planning, or suggestion behavior.

Rules:

- Keep domain terms explicit.
- Separate persisted shape from UI view models.
- Prefer pure functions for rules.
- Add tests for behavior before wiring UI.
- Do not encode core rules only in React components.
